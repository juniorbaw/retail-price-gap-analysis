# Dependances directes du portfolio, versions epinglees.
#
# Volontairement court : ce fichier liste ce que le projet importe reellement,
# pas la sortie brute d'un pip freeze. Les dependances transitives (jinja2,
# agate, protobuf...) sont resolues par pip et n'ont pas a etre figees ici.

# --- Projet 02 : transformation dbt ---
dbt-core==1.12.2
dbt-duckdb==1.11.0
duckdb==1.5.5

# --- Projet 01 : collecte et nettoyage ---
pandas==3.0.5
requests==2.33.1
python-dotenv==1.2.2

# Client de l'API Channel3, utilise par 01-price-positioning-luxury/src/collecte.py.
# Requis uniquement pour rejouer la collecte, pas pour lire les resultats.
channel3_sdk==3.5.0

# --- Export Parquet ---
pyarrow==25.0.1
